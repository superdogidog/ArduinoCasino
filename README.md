buttons and a display are needed for assembly. To build a library (see include). 
The principle is that the more identical numbers there are, the bigger the jackpot. 
The winning coefficient can be adjusted by the variable k.
